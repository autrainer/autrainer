defaults:
  - _autrainer_
  - _self_
  - dataset@evaluation: ToyTabular-C

hydra:
  sweep:
    dir: ${results_dir}/${experiment_id}/evaluation/
    subdir: "\
      ${evaluation.id}_\
      ${checkpoint}_\
      ${dataset.id}_\
      ${model.id}_\
      ${optimizer.id}_\
      ${learning_rate}_\
      ${batch_size}_\
      ${training_type}_\
      ${iterations}_\
      ${scheduler.id}_\
      ${augmentation.id}_\
      ${seed}"
